# Ollama-NetBridge
A lightweight, zero-config tool to share your Ollama instance across a local network. It features a Windows host script that automates firewall management and a cross-platform client that auto-discovers the server, letting you run LLMs on any device as if they were local.
